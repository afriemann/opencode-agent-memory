{
  "name": "opencode-agent-memory",
  "version": "0.1.0",
  "description": "Persistent agent memory plugin for opencode — captures work signals during a session, distils on idle, and auto-injects a context primer at resume time.",
  "type": "module",
  "bin": {
    "agent-memory-tui": "bin/agent-memory-tui"
  },
  "engines": {
    "node": ">=24"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.15.0"
  },
  "devDependencies": {
    "ink-testing-library": "^4.0.0",
    "jest": "^30.5.1"
  },
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "dependencies": {
    "ink": "^7.1.1",
    "react": "^19.3.0"
  }
}
